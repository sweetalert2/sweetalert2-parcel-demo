{
  "name": "sweetalert2-parcel-demo",
  "version": "1.1.0",
  "description": "Demo of using SweetAlert2 with parcel application bundler",
  "license": "MIT",
  "dependencies": {
    "@sweetalert2/themes": "^4.0.1",
    "sweetalert2": "^10.0.0"
  },
  "devDependencies": {
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.9.4",
    "typescript": "^4.0.0"
  },
  "scripts": {
    "start": "parcel watch --no-source-maps app.ts",
    "build": "parcel build --no-source-maps app.ts"
  }
}
